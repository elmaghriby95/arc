# Nginx — allow transaction document uploads up to 400 MB per file
# Include inside the server { } block for the ARC application:
#
#   include /path/to/arc/deploy/nginx-upload-limits.conf;

client_max_body_size 420M;
