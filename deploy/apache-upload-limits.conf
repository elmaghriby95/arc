# ARC — large document uploads (Apache)
# Enable with:
#   sudo a2enconf arc-upload-limits
#   sudo systemctl reload apache2

# Allow request body up to ~420 MB
LimitRequestBody 440401920

# Long uploads (seconds)
Timeout 600
